html, body {
    margin: 0;
    background: white;
    font-family: "Montserrat", sans-serif;

    min-height: 100%;
    height: 100%;
    display: block;
}

body {
    width: clamp(1300px, 100%, 1920px);
    margin: 0 auto;
}
